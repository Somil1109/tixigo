import { createContext, useContext, useEffect, useMemo, useState, type PropsWithChildren } from "react";
import { api } from "../../lib/api";
export type User={id:string;email:string;fullName:string;role:"customer"|"organiser"|"admin";emailVerifiedAt?:string|null}; type Session={data:{accessToken:string;user:User}};
type Value={user:User|null;accessToken:string|null;loading:boolean;login(email:string,password:string):Promise<void>;register(fullName:string,email:string,password:string):Promise<void>;logout():Promise<void>}; const Context=createContext<Value|null>(null);
export function AuthProvider({children}:PropsWithChildren){const[user,setUser]=useState<User|null>(null);const[accessToken,setToken]=useState<string|null>(null);const[loading,setLoading]=useState(true);useEffect(()=>{api<Session>("/auth/refresh",{method:"POST"}).then(r=>{setUser(r.data.user);setToken(r.data.accessToken)}).catch(()=>{}).finally(()=>setLoading(false))},[]);const value=useMemo<Value>(()=>({user,accessToken,loading,async login(email,password){const r=await api<Session>("/auth/login",{method:"POST",body:JSON.stringify({email,password})});setUser(r.data.user);setToken(r.data.accessToken)},async register(fullName,email,password){await api("/auth/register",{method:"POST",body:JSON.stringify({fullName,email,password})})},async logout(){await api("/auth/logout",{method:"POST"});setUser(null);setToken(null)}}),[user,accessToken,loading]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useAuth(){const value=useContext(Context);if(!value)throw new Error("useAuth requires AuthProvider");return value}
