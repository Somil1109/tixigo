# Server
APP_ENV=development
API_PORT=8080
WEB_ORIGIN=http://localhost:5173
DATABASE_URL=postgres://tixigo:tixigo@localhost:5432/tixigo?sslmode=disable

# Authentication
JWT_ACCESS_SECRET=replace-with-a-long-random-secret
JWT_REFRESH_SECRET=replace-with-a-different-long-random-secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=720h

# Email (Resend in production; Mailpit for local development)
EMAIL_FROM=Tixigo <noreply@localhost>
RESEND_API_KEY=
MAILPIT_HOST=localhost
MAILPIT_PORT=1025

# Media
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
